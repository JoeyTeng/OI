#include<bits/stdc++.h>

int N;
long long H;
std::vector<long long> S;

int main() {
	std::cin >> N >> H;
	for (int i = 0; i < N; ++i) {
		int tmp;
		std::cin >> tmp;
		S.push_back(tmp);
	}
	
	return 0;
}
