// lights

// Mathematics
// a number will have odd number of factors if it is in the form of x^2, where
// x\in \doubleZ

#include <bits/stdc++.h>

int main() {
    std::int64_t N;

    std::cin >> N;
    std::cout << std::int64_t(std::sqrt(N)) << std::endl;

    return 0;
}
